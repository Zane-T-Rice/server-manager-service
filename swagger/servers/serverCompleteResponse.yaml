description: Success
content:
  application/json:
    schema:
      allOf:
        - type: object
          properties:
            ports:
              type: array
              items:
                $ref: "../ports/portResponseSchema.yaml"
        - $ref: "./serverResponseSchema.yaml"
